<section class='hide-overflow tab inventory {{tab.cssClass}}' data-group='primary' data-tab='inventory'>
    <div class="flexrow">
        {{> equipmentSlots}}
        {{> equipmentLoadouts}}
    </div>
    {{> inventory}}
</section>