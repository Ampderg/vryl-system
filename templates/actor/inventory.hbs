<section class='tab inventory {{#with (lookup tabs "inventory")}} {{cssClass}} {{/with}}' style="overflow-x: visible;" data-group='primary' data-tab='inventory'>
    <div class="flexrow">
        {{> equipmentSlots}}
        {{> equipmentLoadouts}}
    </div>
    {{> inventory}}
</section>